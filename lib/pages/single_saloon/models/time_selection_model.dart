class TimeSelectionModel {
  String timeString;
  DateTime time; // New property to store the actual time

  TimeSelectionModel({
    required this.timeString,
    required this.time, // Update constructor
  });
}
